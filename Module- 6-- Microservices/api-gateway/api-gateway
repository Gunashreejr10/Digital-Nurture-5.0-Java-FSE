
Generate a third project:
Artifact: api-gateway
Dependencies: Gateway, Eureka Discovery Client, Spring Boot Actuator, DevTools

application.properties:

properties
server.port=9090
spring.application.name=api-gateway
spring.cloud.gateway.discovery.locator.enabled=true
spring.cloud.gateway.discovery.locator.lower-case-service-id=true

discovery.locator.enabled=true means the gateway auto-creates routes based on whatever's registered in Eureka
